# 第二轮交互修订

基于 `papertodo-compact-updated-source.zip`。本轮关于关联笔记的要求取代上一轮的“周末计划独立胶囊”规则。

1. 周末计划不在侧边队列显示。保留原有笔记和待办关联；展开与收起的动画起终点使用今日待办中的关联按钮，Markdown 章节临时隐藏待办时仍保留该位置，不退回边缘。关闭笔记后键盘焦点回到关联入口。
2. 工具制作只允许选择专注计时、习惯打卡、待办清单三个预设。默认选择专注计时，展示所选预设的说明；不再提供自由输入、字符计数或 Ctrl / ⌘ + Enter 提示和快捷键。
3. 脚本纸片和胶囊预览的顶栏增加运行键。结果窗口只提供“关闭”和“回到纸片”：关闭隐藏结果并保留脚本胶囊，回到纸片恢复脚本正文；运行中也可退出并取消等待中的结果更新。
4. 删除工具演示的“导出与源码”区块、HTML 下载入口和 Codex CLI Bridge 链接。
5. Markdown 关闭模式使用独立纯文本容器，保留文字与换行，不应用代码块背景、边框或内边距。基础和完全渲染模式保留原有样式。
6. 删除顶栏品牌名下方的“小小纸片，不止于纸。”，以及页脚的旧站归档、当前公开官网、网站实验源码三个链接。
7. 脚本结果窗口标题统一为 `powershell`，中英文相同。

侧边现在是三个固定入口与一个生成工具位置；新工具成功制作后仍替换上一工具。上一轮底栏、主胶囊、右箭头及三个并排按钮的修改继续保留。

当前验证记录见 `evidence/LOCAL-V2-VALIDATION.json`。历史报告保留，但不代表本次运行结果；本次未推送或部署。
